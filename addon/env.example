# Stremio NAS Addon Configuration
# Copy this file to .env and adjust values as needed
# cp env.example .env (Linux/Mac)
# copy env.example .env (Windows)

# ============================================
# API Configuration
# ============================================

# URL for addon to connect to API server
# Also used as base URL for stream URLs that Stremio will use
# - For Docker: use service name (e.g., http://stremio-nas-api:3000)
# - For external: use your NAS IP (e.g., http://192.168.1.100:3000)
# Must be accessible from wherever Stremio is running
API_URL=http://localhost:3000

# ============================================
# API Security Configuration
# ============================================

# API Key for authentication (optional)
# Required if the server has API_KEY configured
# Must match the API_KEY set in the server environment
# Leave empty if server doesn't require authentication
API_KEY=

# ============================================
# Server Configuration
# ============================================

# Port for the Stremio addon HTTP server
PORT=1222

# ============================================
# Polling Configuration
# ============================================

# Interval to poll API for file updates (in minutes)
POLL_INTERVAL_MINUTES=5

# Enable periodic polling (true/false)
# Set to false to disable periodic polling (only indexes on startup)
ENABLE_POLLING=true

# ============================================
# Indexing Configuration
# ============================================

# Maximum number of files to index in memory (safety limit)
MAX_INDEXED=10000

# ============================================
# Logging Configuration
# ============================================

# Logging level: error, warn, info, debug
LOG_LEVEL=debug

